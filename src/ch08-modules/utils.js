function sum(num1, num2) {
  return num1 + num2;
}
export { sum as add };

function subtract(num1, num2) {
  return num1 - num2;
}
export default subtract;
// alternative syntax
// export { subtract as default;
